[project]
name = "wl-saga-sidecar"
version = "0.1.0"
description = "SAGA scheduler sidecar for the Wayline odag-controller"
requires-python = ">=3.12"
dependencies = [
    "anrg-saga>=2.1.0",
]

[dependency-groups]
dev = ["pytest>=8"]
