apiVersion: wl.io/v1
kind: ODAGTemplate
metadata:
  name: rag-refresh
  namespace: wl-system
spec:
  description: "RAG KB refresh with a real ONNX embedding model: prepare-model (cached across runs) + ingest -> chunk -> embed -> index -> merge -> eval -> report"
  scheduler: heft
  profiling:
    enabled: true
    warmupRuns: 0
    minSamples: 2
    emaAlpha: 0.3
    maxSamples: 50
  defaults:
    runtime: 10
    dataSize: "5MB"
  retention:
    maxRuns: 10
    data:
      policy: keepLatest
      keepRuns: 3
      maxSizePerNode: "2Gi"
  tasks:
    # ====================================================================
    # Layer 0a — Prepare model (cross-run reuse via cacheKey)
    # Materializes the ONNX MiniLM-L6-v2 model (~90MB) as a data object.
    # After run 1 the controller binds this task to the previous run's
    # installed copy: no re-materialization, and the scheduler places
    # the embed/eval consumers near the cached bytes.
    # ====================================================================

    - name: prepare-model
      image: 192.168.1.163:5000/wl-rag-model:latest
      command: ["python", "task.py"]
      dependencies: []
      dataSize: "90MB"
      runtime: 6
      cacheKey: rag-minilm-l6-v2-onnx-v1
      resources:
        cpu: "300m"
        memory: "512Mi"
      constraints:
        nodeNames: [anrg-3, anrg-4, anrg-5]

    # ====================================================================
    # Layer 0 — Ingest & Shard (data-local node)
    # Generates synthetic corpus, packs 4 shards into a single blob.
    # All 4 chunk tasks receive the same blob and extract their shard.
    # ====================================================================

    - name: ingest-shard
      image: 192.168.1.163:5000/wl-rag-ingest:latest
      command: ["python", "task.py"]
      dependencies: []
      dataSize: "8MB"
      runtime: 5
      resources:
        cpu: "300m"
        memory: "256Mi"
      constraints:
        nodeNames: [anrg-1]        # data-local node

    # ====================================================================
    # Layer 1 — Chunk (co-located with ingest for data locality)
    # Each chunk-shard-i extracts its shard and splits into passages.
    # ====================================================================

    - name: chunk-shard-1
      image: 192.168.1.163:5000/wl-rag-chunk:latest
      command: ["python", "task.py"]
      dependencies: ["ingest-shard"]
      dataSize: "2500KB"
      runtime: 5
      resources:
        cpu: "300m"
        memory: "256Mi"
      constraints:
        nodeNames: [anrg-1]        # same as ingest (data locality)

    - name: chunk-shard-2
      image: 192.168.1.163:5000/wl-rag-chunk:latest
      command: ["python", "task.py"]
      dependencies: ["ingest-shard"]
      dataSize: "2500KB"
      runtime: 5
      resources:
        cpu: "300m"
        memory: "256Mi"
      constraints:
        nodeNames: [anrg-1]

    - name: chunk-shard-3
      image: 192.168.1.163:5000/wl-rag-chunk:latest
      command: ["python", "task.py"]
      dependencies: ["ingest-shard"]
      dataSize: "2500KB"
      runtime: 5
      resources:
        cpu: "300m"
        memory: "256Mi"
      constraints:
        nodeNames: [anrg-1]

    - name: chunk-shard-4
      image: 192.168.1.163:5000/wl-rag-chunk:latest
      command: ["python", "task.py"]
      dependencies: ["ingest-shard"]
      dataSize: "2500KB"
      runtime: 5
      resources:
        cpu: "300m"
        memory: "256Mi"
      constraints:
        nodeNames: [anrg-1]

    # ====================================================================
    # Layer 2 — Embed (feasible-node constraint: embedding-capable nodes)
    # Feature-hash embeddings (D=128). CPU busy loop for runtime budget.
    # ====================================================================

    - name: embed-shard-1
      image: 192.168.1.163:5000/wl-rag-embed:latest
      command: ["python", "task.py"]
      dependencies: ["chunk-shard-1", "prepare-model"]
      dataSize: "8MB"
      runtime: 120
      resources:
        cpu: "3"
        memory: "1Gi"
      constraints:
        nodeNames: [anrg-6, anrg-7, anrg-8, anrg-9]

    - name: embed-shard-2
      image: 192.168.1.163:5000/wl-rag-embed:latest
      command: ["python", "task.py"]
      dependencies: ["chunk-shard-2", "prepare-model"]
      dataSize: "8MB"
      runtime: 120
      resources:
        cpu: "3"
        memory: "1Gi"
      constraints:
        nodeNames: [anrg-6, anrg-7, anrg-8, anrg-9]

    - name: embed-shard-3
      image: 192.168.1.163:5000/wl-rag-embed:latest
      command: ["python", "task.py"]
      dependencies: ["chunk-shard-3", "prepare-model"]
      dataSize: "8MB"
      runtime: 120
      resources:
        cpu: "3"
        memory: "1Gi"
      constraints:
        nodeNames: [anrg-6, anrg-7, anrg-8, anrg-9]

    - name: embed-shard-4
      image: 192.168.1.163:5000/wl-rag-embed:latest
      command: ["python", "task.py"]
      dependencies: ["chunk-shard-4", "prepare-model"]
      dataSize: "8MB"
      runtime: 120
      resources:
        cpu: "3"
        memory: "1Gi"
      constraints:
        nodeNames: [anrg-6, anrg-7, anrg-8, anrg-9]

    # ====================================================================
    # Layer 3 — Build Index (unconstrained, scheduler picks)
    # Adds coarse bucket inverted index on top of vectors.
    # ====================================================================

    - name: build-index-1
      image: 192.168.1.163:5000/wl-rag-index:latest
      command: ["python", "task.py"]
      dependencies: ["embed-shard-1"]
      dataSize: "8MB"
      runtime: 3
      resources:
        cpu: "300m"
        memory: "512Mi"

    - name: build-index-2
      image: 192.168.1.163:5000/wl-rag-index:latest
      command: ["python", "task.py"]
      dependencies: ["embed-shard-2"]
      dataSize: "8MB"
      runtime: 3
      resources:
        cpu: "300m"
        memory: "512Mi"

    - name: build-index-3
      image: 192.168.1.163:5000/wl-rag-index:latest
      command: ["python", "task.py"]
      dependencies: ["embed-shard-3"]
      dataSize: "8MB"
      runtime: 3
      resources:
        cpu: "300m"
        memory: "512Mi"

    - name: build-index-4
      image: 192.168.1.163:5000/wl-rag-index:latest
      command: ["python", "task.py"]
      dependencies: ["embed-shard-4"]
      dataSize: "8MB"
      runtime: 3
      resources:
        cpu: "300m"
        memory: "512Mi"

    # ====================================================================
    # Layer 4 — Merge Index (fan-in from all 4 build-index tasks)
    # ====================================================================

    - name: merge-index
      image: 192.168.1.163:5000/wl-rag-merge:latest
      command: ["python", "task.py"]
      dependencies: ["build-index-1", "build-index-2", "build-index-3", "build-index-4"]
      dataSize: "32MB"
      runtime: 5
      resources:
        cpu: "300m"
        memory: "1Gi"
      constraints:
        nodeNames: [anrg-8, anrg-9]

    # ====================================================================
    # Layer 5 — Eval Queries (brute-force retrieval + latency stats)
    # ====================================================================

    - name: eval-queries
      image: 192.168.1.163:5000/wl-rag-eval:latest
      command: ["python", "task.py"]
      dependencies: ["merge-index", "prepare-model"]
      dataSize: "1MB"
      runtime: 15
      resources:
        cpu: "500m"
        memory: "1Gi"
      constraints:
        nodeNames: [anrg-8, anrg-9]

    # ====================================================================
    # Layer 6 — Report (gateway node)
    # ====================================================================

    - name: report
      image: 192.168.1.163:5000/wl-rag-report:latest
      command: ["python", "task.py"]
      dependencies: ["eval-queries"]
      dataSize: "0"
      runtime: 2
      resources:
        cpu: "200m"
        memory: "128Mi"
      constraints:
        nodeNames: [anrg-9]        # gateway node
