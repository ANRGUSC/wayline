"""
mcmt computation library.

Pure functions only. No Wayline SDK imports. No Argo conventions. Each module
takes paths to extracted input directories and produces files in an output
directory. The Wayline and Argo wrappers handle tar.gz pack/unpack at the
stage boundary so the lib stays uniform.
"""
